# Sources

This directory contains raw source snapshots used for the report. They are intentionally local copies so the project remains readable if GitHub search or docs pages change.

Sensitive note: do not add API keys or auth config exports here. GitHub issue #20083 is a reminder that Google API keys pasted into public issues are detected and should be rotated.
